{
  "name": "pi-coding-agent-repo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "extensions/*",
    "examples/extensions/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "check": "npm run build && npm run test",
    "dev": "tsx packages/cli/src/index.ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.0.3",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  }
}
